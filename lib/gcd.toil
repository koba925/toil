deffunc gcd_recur params a, b do
    if a == 0 then b else gcd_recur(b % a, a) end
end;

deffunc gcd_iter params a, b do
    while a != 0 do [a, b] := [b % a, a] end;
    b
end;

[gcd_recur, gcd_iter]
